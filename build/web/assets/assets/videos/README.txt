Drop a looping ocean/beach video here named exactly:

    hero_wave.mp4

It will automatically be used as the hero background (muted, looping) the
next time you hot-restart the app. If this file is missing, the app
falls back to an animated gradient + wave-painter background, so
everything keeps working with zero setup.

Tips for the video file itself:
- Short clip (5-15s), seamless loop, no audio needed (it's played muted).
- Landscape orientation, roughly 16:9, keep the file size reasonable
  (a few MB) for fast loading on mobile.
